{
  "tasks": [
    {
      "id": "bravo-20260708065238",
      "title": "Убрать захардкоженный HTTP backend (`http://18.153.141.210`) из `bld-panel/vercel.json` и перевести проксирование на HTTPS/переменные окружения",
      "squad": "bravo",
      "status": "proposed",
      "reason": "Сейчас фронт при деплое может проксировать API на фиксированный IP по нешифрованному HTTP, что создаёт риск MITM/подмены ответов и утечки токенов/данных. Плюс это ломает надёжность: смена IP/окружения потребует коммита и может внезапно уронить прод.",
      "how": "Заменить `destination` в `vercel.json` на адрес из env (например `API_BASE_URL`) и требовать `https://` в проде. Добавить проверку/guard в CI (grep) чтобы запретить `destination` с `http://` и голыми IP, и обновить документацию по настройке окружений.",
      "created": "08.07.2026 06:52",
      "cto_comment": ""
    },
    {
      "id": "bravo-20260708092651",
      "title": "В `bld-system/core/owner_activation.py` убрать небезопасный дефолт `http://localhost:{PORT}` для OWNER_ACTIVATION_BASE_URL (не допускать генерацию activation-ссылок на localhost/HTTP вне dev)",
      "squad": "bravo",
      "status": "done",
      "reason": "Сейчас при пустом `OWNER_ACTIVATION_BASE_URL` код генерирует ссылки вида `http://localhost:{PORT}`, что опасно: в misconfig окружении такие ссылки могут уйти реальным пользователям, сломав активацию и создавая риск утечки/подмены через не-TLS URL. Это также усложняет мониторинг инцидентов (трудно отличить dev от prod по поведению).",
      "how": "Изменить `get_owner_activation_base_url()` так, чтобы дефолт `http://localhost:{PORT}` использовался только при явном `environment=development` (или аналогичном флаге), а во всех остальных окружениях — выбрасывалась ошибка/логировался критический конфиг. Добавить тест, который гарантирует: в production/staging без `OWNER_ACTIVATION_BASE_URL` функция падает, а в development — возвращает localhost.",
      "created": "08.07.2026 09:26",
      "cto_comment": "Проверил код: `core/owner_activation.py` сейчас действительно отдаёт `http://localhost:{PORT}` для любого окружения, кроме `production`, то есть staging/misconfig легко получит битые customer-facing activation-ссылки. Дубля в работе нет — это ровно задача из pending approval, в вики такого решения не зафиксировано. Чинить просто: использовать уже существующий `settings.ENVIRONMENT`, разрешить localhost только для `development`, а в staging/production падать тестируемой ошибкой; новый флаг не городить."
    },
    {
      "id": "bravo-20260708141021",
      "title": "В `bld-panel/vite.config.js` убрать небезопасный дефолт dev-прокси `target: 'http://localhost:8000'` и запретить HTTP/localhost в non-dev окружениях",
      "squad": "bravo",
      "status": "rejected",
      "reason": "Сейчас при отсутствии `VITE_DEV_PROXY_TARGET` фронтенд по умолчанию проксирует API на `http://localhost:8000`, что может привести к случайным деплоям/прогонам (preview/CI) с запросами в localhost по HTTP. Это риск SSRF/ошибочного маршрутизации и источник трудноотлавливаемых сбоев, если конфиг окружения не задан строго.",
      "how": "Сделать `VITE_DEV_PROXY_TARGET` обязательным для режима `development`, а в `preview/production` полностью отключать dev-proxy. Добавить валидацию: запрещать `http://` и `localhost/127.0.0.1` вне dev, при нарушении — падать с явной ошибкой сборки/старта.",
      "created": "08.07.2026 14:10",
      "cto_comment": "В `vite.config.js` этот `target: env.VITE_DEV_PROXY_TARGET || 'http://localhost:8000'` находится в `server.proxy`, то есть это Vite dev-server конфиг, а не production routing; формулировка про SSRF/случайный prod-деплой выглядит раздутой. Переформулировать задачу стоит так: отключить/не объявлять proxy вне `mode === 'development'` и добавить явную ошибку, если dev-proxy почему-то включён в non-dev, но не делать `VITE_DEV_PROXY_TARGET` обязательным для локальной разработки — localhost в dev нормален."
    }
  ],
  "last_updated": "08.07.2026 14:10"
}